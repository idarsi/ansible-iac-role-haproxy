---
dependency:
  name: "galaxy"
driver:
  name: "podman"
platforms:
  - name: "rocky9"
    image: "rockylinux:9"
    pre_build_image: true
provisioner:
  name: "ansible"
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_PROJECT_DIRECTORY}/.."
verifier:
  name: "ansible"
